{% layout 'layout.liquid' %}
{% block content %}
  <div class="container-md">
    <article>
      <h2>
        {{ theme.title }}
        -
        {% if theme.ready %}
          Ready
        {% else %}
          Not Ready
        {%endif%}
      </h2>
      <h3>Amount:
        {{ theme.amount }}
      </h3>
      <h6>Body:
        {{ theme.body }}
      </h6>
      {% if theme.owner == userId %}
        <div class="btn-group">

          <a href="/themes/{{ theme.id }}/edit" class="btn btn-primary">Edit</a>
          <form action="/theme/{{ theme.id }}?_method=DELETE" method="POST">
            <input type="submit" value="Delete" class="btn btn-danger">
          </form>
        </div>
      {% endif %}
    </article>
  </div>
{% endblock %}
